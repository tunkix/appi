# Plan: Install PHPStan and verify PHPUnit

## Current State
- **PHPUnit**: Already installed (v11.5.55, in `require-dev`). Config at `phpunit.dist.xml`. Stock tests in `tests/`.
- **PHPStan**: Not installed. No package, no config.

## Steps

### 1. Install PHPStan + CI4 extension
```bash
composer require --dev phpstan/phpstan codeigniter/phpstan-codeigniter phpstan/phpstan-phpunit
```

### 2. Create `phpstan.neon.dist`
- Level 5 (balanced strictness for CI4)
- Paths: `app/` (Modules, Controllers, Models, Services, Filters, Config)
- Includes: CI4 extension + PHPUnit extension
- Scan `app/Modules/*/` for module code

### 3. Add Composer scripts
```json
"analyse": "phpstan analyse",
"analyse-baseline": "phpstan analyse --generate-baseline"
```

### 4. Verify
- `./vendor/bin/phpunit` — tests still pass
- `composer analyse` — run PHPStan, fix or baseline any errors

## Files Modified
- `composer.json` — add packages + scripts
- `phpstan.neon.dist` — new config (project root)
